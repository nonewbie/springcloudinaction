spring:
  application:
    name: config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      label: feature_configserver
  profiles:
    active: pro

eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
