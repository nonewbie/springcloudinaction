turbine:
  aggregator:
    clusterConfig: default
  app-config: uaa-service,user-service
  cluster-name-expression: new String("default")

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8762
spring:
  application:
    name: monitor-turbine